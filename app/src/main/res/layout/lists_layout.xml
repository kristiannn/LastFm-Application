<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        style="@style/ProgressBarHorizontal"
        android:layout_width="match_parent"
        android:layout_height="16dp"
        android:layout_marginTop="-6dp"/>

    <Button
        android:id="@+id/buttonScrobble"
        style="@style/Button.Rounded"
        android:layout_width="@dimen/scrobble_button_width"
        android:layout_height="@dimen/scrobble_button_height"
        android:layout_gravity="end|bottom"
        android:layout_marginEnd="@dimen/scrobble_button_end_margin"
        android:layout_marginBottom="@dimen/scrobble_button_bottom_margin"
        android:visibility="gone"/>

</FrameLayout>